// Configuration example for local admin (prototype)
// Copy to assets/js/config.admin.js and set credentials for the local admin.
window.SITEMAINT_ADMIN_CONFIG = {
  ADMIN_USER: "admin",
  ADMIN_PASS: "admin123"
};
